node('nodejs8') {
  stage 'checkout'
    checkout scm
  stage 'build'
  sh '''
    pwd
    ls -l
    npm install -d
    $(npm bin)/ng build --prod --build-optimizer
    oc whoami
    oc login -u=developer -p=developer
    oc status
  '''
}
